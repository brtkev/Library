<mvc:View xmlns:core="sap.ui.core"
		controllerName="root.controller.BookList"
		xmlns="sap.m"
		xmlns:mvc="sap.ui.core.mvc">
	<Panel accessibleRole="Region">
		<headerToolbar>
			<Toolbar>
				<Title text="{i18n>SearchBooktitle}"/>
				<ToolbarSpacer/>
				<Text text="search by"></Text>
				<Select selectedKey="1" id="bookListSelect" >
				 <items>
				  <core:Item key="0" text="id"></core:Item>
				  <core:Item key="1" text="title"></core:Item>
				  <core:Item key="2" text="subtitle"></core:Item>
				  <core:Item key="3" text="author"></core:Item>
				  <core:Item key="4" text="category"></core:Item>
				  <core:Item key="5" text="print date"></core:Item>
				  <core:Item key="6" text="editor"></core:Item>
				 </items>
				</Select>
				<SearchField
					width="50%"
					search=".onFilterBooks"
					ariaLabelledBy="searchFieldLabel"
					ariaDescribedBy="searchFieldDescription"
					placeholder="{i18n>searchFieldPlaceholder}"/>
			</Toolbar>
		</headerToolbar>
		<Table
			id="bookList"
			class="sapUiResponsiveMargin"
			width="auto"
			items="{path: '/books'}">
			<columns>
				<Column
					hAlign="End"
					minScreenWidth="Small"
					demandPopin="true"
					width="4em">
					<Text text="{i18n>columnId}"/>
				</Column>
				<Column>
					<Text text="{i18n>columnTitle}"/>
				</Column>
				<Column>
					<Text text="{i18n>columnSubtitle}"/>
				</Column>
				<Column
					minScreenWidth="Small"
					demandPopin="true">
					<Text text="{i18n>columnAuthor}"/>
				</Column>
				<Column
					minScreenWidth="Tablet"
					demandPopin="false">
					<Text text="{i18n>columnCategory}"/>
				</Column>
				<Column
					hAlign="End">
					<Text text="{i18n>columnPrintDate}"/>
				</Column>
				<Column
					hAlign="End">
					<Text text="{i18n>columnEditor}"/>
				</Column>
			</columns>
			<items>
				<ColumnListItem
					type="Navigation"
					press=".onPress">
					<cells>
						<ObjectNumber number="{book_id}" emphasized="false"/>
						<ObjectIdentifier title="{title}"/>
						<Text text="{subtitle}"/>
						<Text text="{author}"/>
						<Text text="{category}"/>
						<Text text="{printdate}"/>
						<Text text="{editor}"/>
						<!-- <ObjectNumber
							number="{
								parts: [{path: 'invoice>ExtendedPrice'}, {path: 'view>/currency'}],
								type: 'sap.ui.model.type.Currency',
								formatOptions: {
									showMeasure: false
								}
							}"
							unit="{view>/currency}"
							state="{= ${invoice>ExtendedPrice} > 50 ? 'Error' : 'Success' }"/> -->
					</cells>
				</ColumnListItem>
			</items>
		</Table>
	</Panel>
</mvc:View>